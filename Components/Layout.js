const Layout = ({children, title = "Crypto Tracker"}) => {
    <div className="layout">
        <Head>
            <title>{title}</title>
            <link rel="icon" href="./favicon.png" />
        </Head>
    </div>
}